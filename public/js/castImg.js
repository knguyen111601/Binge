if ($(`p:contains("Edward Norton")`)) {
    const img = $(`p:contains("Edward Norton")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/YjHC3wd.jpg").addClass("cOneImg"));
}

// Liv Tyler
if ($(`p:contains("Liv Tyler")`)) {
    const img = $(`p:contains("Liv Tyler")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/DrAdBGH.jpg").addClass("cOneImg"));

}

// Tim Roth
if ($(`p:contains("Tim Roth")`)) {
    const img = $(`p:contains("Tim Roth")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/5TmVbYt.jpg").addClass("cOneImg"));
}

// Robert Downey Jr.
if ($(`p:contains("Robert Downey Jr.")`)) {
    const img = $(`p:contains("Robert Downey Jr.")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/GdrqTEt.jpg").addClass("cOneImg"));
}

// Jon Favreau
if ($(`p:contains("Jon Favreau")`)) {
    const img = $(`p:contains("Jon Favreau")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/e3kCI9n.jpg").addClass("cOneImg"));
}

// Gwyneth Paltrow
if ($(`p:contains("Gwyneth Paltrow")`)) {
    const img = $(`p:contains("Gwyneth Paltrow")`)
    img.prepend($("<img>").attr("src", "https://m.media-amazon.com/images/M/MV5BNzIxOTQ1NTU1OV5BMl5BanBnXkFtZTcwMTQ4MDY0Nw@@._V1_UX214_CR0,0,214,317_AL_.jpg").addClass("cOneImg"));
}

// Chris Hemsworth
if ($(`p:contains("Chris Hemsworth")`)) {
    const img = $(`p:contains("Chris Hemsworth")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/z25iWxd.jpg").addClass("cOneImg"));
}

// Natalie Portman
if ($(`p:contains("Natalie Portman")`)) {
    const img = $(`p:contains("Natalie Portman")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/mWdkgCk.jpg").addClass("cOneImg"));
}

// Tom Hiddleston
if ($(`p:contains("Tom Hiddleston")`)) {
    const img = $(`p:contains("Tom Hiddleston")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/EvCUInR.jpg").addClass("cOneImg"));
}

// Chris Evans
if ($(`p:contains("Chris Evans")`)) {
    const img = $(`p:contains("Chris Evans")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/OoIbvkj.jpg").addClass("cOneImg"));
}

// Haley Atwell
if ($(`p:contains("Haley Atwell")`)) {
    const img = $(`p:contains("Haley Atwell")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/LCetzxk.jpg").addClass("cOneImg"));
}

// Sebastian Stan
if ($(`p:contains("Sebastian Stan")`)) {
    const img = $(`p:contains("Sebastian Stan")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/8KdFHbS.jpg").addClass("cOneImg"));
}

// Don Cheadle
if ($(`p:contains("Don Cheadle")`)) {
    const img = $(`p:contains("Don Cheadle")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/oTVAFqx.jpg").addClass("cOneImg"));
}

// Ben Kingsley
if ($(`p:contains("Ben Kingsley")`)) {
    const img = $(`p:contains("Ben Kingsley")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/oHXTloF.jpg").addClass("cOneImg"));
}

// Anthony Mackie
if ($(`p:contains("Anthony Mackie")`)) {
    const img = $(`p:contains("Anthony Mackie")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/iuRAJMj.jpg").addClass("cOneImg"));
}

// James Spador
if ($(`p:contains("James Spador")`)) {
    const img = $(`p:contains("James Spador")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/7FoBi43.jpg").addClass("cOneImg"));
}

// Scarlett Johansson
if ($(`p:contains("Scarlett Johansson")`)) {
    const img = $(`p:contains("Scarlett Johansson")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/TW0VOek.jpg").addClass("cOneImg"));
}

// Paul Rudd
if ($(`p:contains("Paul Rudd")`)) {
    const img = $(`p:contains("Paul Rudd")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/f8UcqnA.jpg").addClass("cOneImg"));
}

// Evangeline Lilly
if ($(`p:contains("Evangeline Lilly")`)) {
    const img = $(`p:contains("Evangeline Lilly")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/S23Opn7.jpg").addClass("cOneImg"));
}

// Michael Douglas
if ($(`p:contains("Michael Douglas")`)) {
    const img = $(`p:contains("Michael Douglas")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/4MWdRlm.jpg").addClass("cOneImg"));
}

// Tom Holland
if ($(`p:contains("Tom Holland")`)) {
    const img = $(`p:contains("Tom Holland")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/SaW6l6q.jpg").addClass("cOneImg"));
}

// Benedict Cumberbatch
if ($(`p:contains("Benedict Cumberbatch")`)) {
    const img = $(`p:contains("Benedict Cumberbatch")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/Z06v961.jpg").addClass("cOneImg"));
}

// Rachel McAdams
if ($(`p:contains("Rachel McAdams")`)) {
    const img = $(`p:contains("Rachel McAdams")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/0OUsAqh.jpg").addClass("cOneImg"));
}

// Tilda Swinton
if ($(`p:contains("Rachel McAdams")`)) {
    const img = $(`p:contains("Rachel McAdams")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/SXeWod3.jpg").addClass("cOneImg"));
}

// Chris Pratt
if ($(`p:contains("Chris Pratt")`)) {
    const img = $(`p:contains("Chris Pratt")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/mW56fad.jpg").addClass("cOneImg"));
}

// Dave Bautista
if ($(`p:contains("Dave Bautista")`)) {
    const img = $(`p:contains("Dave Bautista")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/mfOkoT0.jpg").addClass("cOneImg"));
}

// Zoe Saldana
if ($(`p:contains("Zoe Saldana")`)) {
    const img = $(`p:contains("Zoe Saldana")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/MSZlgHp.jpg").addClass("cOneImg"));
}

// Michael Keaton
if ($(`p:contains("Michael Keaton")`)) {
    const img = $(`p:contains("Michael Keaton")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/XMNTmy2.jpg").addClass("cOneImg"));
}

// Zendaya
if ($(`p:contains("Zendaya")`)) {
    const img = $(`p:contains("Zendaya")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/uxnx6Si.jpg").addClass("cOneImg"));
}

// Tessa Thompson
if ($(`p:contains("Tessa Thompson")`)) {
    const img = $(`p:contains("Tessa Thompson")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/yRxK6TT.jpg").addClass("cOneImg"));
}

// Taika Waititi
if ($(`p:contains("Taika Waititi")`)) {
    const img = $(`p:contains("Taika Waititi")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/0KsT10g.jpg").addClass("cOneImg"));
}

// Chadwick Boseman
if ($(`p:contains("Chadwick Boseman")`)) {
    const img = $(`p:contains("Chadwick Boseman")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/hBf7nUc.jpg").addClass("cOneImg"));
}

// Michael B. Jordan
if ($(`p:contains("Michael B. Jordan")`)) {
    const img = $(`p:contains("Michael B. Jordan")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/l8f81sQ.jpg").addClass("cOneImg"));
}

// Lupita Nyong'o
if ($(`p:contains("Lupita Nyong'o")`)) {
    const img = $(`p:contains("Lupita Nyong'o")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/6GdMDfE.jpg").addClass("cOneImg"));
}

// Simu Liu
if ($(`p:contains("Simu Liu")`)) {
    const img = $(`p:contains("Simu Liu")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/gG3B62c.jpg").addClass("cOneImg"));
}

// Awkwafina
if ($(`p:contains("Awkwafina")`)) {
    const img = $(`p:contains("Awkwafina")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/vjFHsHj.jpg").addClass("cOneImg"));
}

// Tony Leung Chiu-wai
if ($(`p:contains("Tony Leung Chiu-wai")`)) {
    const img = $(`p:contains("Tony Leung Chiu-wai")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/cBnBvvJ.jpg").addClass("cOneImg"));
}

// Alfred Molina
if ($(`p:contains("Alfred Molina")`)) {
    const img = $(`p:contains("Alfred Molina")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/1f4R8yW.jpg").addClass("cOneImg"));
}

// Jake Gyllenhaal
if ($(`p:contains("Jake Gyllenhaal")`)) {
    const img = $(`p:contains("Jake Gyllenhaal")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/kFOjerB.jpg").addClass("cOneImg"));
}

// Gemma Chan
if ($(`p:contains("Gemma Chan")`)) {
    const img = $(`p:contains("Gemma Chan")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/zOBeKn8.jpg").addClass("cOneImg"));
}

// Angelina Jolie
if ($(`p:contains("Angelina Jolie")`)) {
    const img = $(`p:contains("Angelina Jolie")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/w9EbKSK.jpg").addClass("cOneImg"));
}

// Kumail Nanjiani
if ($(`p:contains("Kumail Nanjiani")`)) {
    const img = $(`p:contains("Kumail Nanjiani")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/1JkP0ua.jpg").addClass("cOneImg"));
}

// Florence Pugh
if ($(`p:contains("Florence Pugh")`)) {
    const img = $(`p:contains("Florence Pugh")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/UZeQ0ZI.jpg").addClass("cOneImg"));
}

// David Harbour
if ($(`p:contains("David Harbour")`)) {
    const img = $(`p:contains("David Harbour")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/i0np02t.jpg").addClass("cOneImg"));
}

// Brie Larson
if ($(`p:contains("Brie Larson")`)) {
    const img = $(`p:contains("Brie Larson")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/ezIJsZ9.jpg").addClass("cOneImg"));
}

// Samuel L. Jackson
if ($(`p:contains("Samuel L. Jackson")`)) {
    const img = $(`p:contains("Samuel L. Jackson")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/LQkP4tB.jpg").addClass("cOneImg"));
}

// Ben Mendelsohn
if ($(`p:contains("Ben Mendelsohn")`)) {
    const img = $(`p:contains("Ben Mendelsohn")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/h76xkpz.jpg").addClass("cOneImg"));
}

// Jeffery Wright
if ($(`p:contains("Jeffery Wright")`)) {
    const img = $(`p:contains("Jeffery Wright")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/knFgCd6.jpg").addClass("cOneImg"));
}

// Jeremy Renner
if ($(`p:contains("Jeremy Renner")`)) {
    const img = $(`p:contains("Jeremy Renner")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/ljAuXTF.jpg").addClass("cOneImg"));
}

// Wyatt Russell
if ($(`p:contains("Wyatt Russell")`)) {
    const img = $(`p:contains("Wyatt Russell")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/DOvwzNo.jpg").addClass("cOneImg"));
}

// Owen Wilson
if ($(`p:contains("Owen Wilson")`)) {
    const img = $(`p:contains("Owen Wilson")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/wewk50C.jpg").addClass("cOneImg"));
}

// Sophia Di Martino
if ($(`p:contains("Sophia Di Martino")`)) {
    const img = $(`p:contains("Sophia Di Martino")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/ICMBEXa.jpg").addClass("cOneImg"));
}

// Elizabeth Olsen
if ($(`p:contains("Elizabeth Olsen")`)) {
    const img = $(`p:contains("Elizabeth Olsen")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/qJOF81o.jpg").addClass("cOneImg"));
}

// Paul Bettany
if ($(`p:contains("Paul Bettany")`)) {
    const img = $(`p:contains("Paul Bettany")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/fEU2lXP.jpg").addClass("cOneImg"));
}

// Kathryn Hahn
if ($(`p:contains("Kathryn Hahn")`)) {
    const img = $(`p:contains("Kathryn Hahn")`)
    img.prepend($("<img>").attr("src", "https://i.imgur.com/HrotfhH.jpg").addClass("cOneImg"));
}